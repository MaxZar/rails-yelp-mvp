<h1>List of restaurants</h1>

<ul>
  <% @restaurants.each do |resto| %>
    <li><%= link_to resto.name, restaurant_path(resto) %> <%= @averages[resto] %></li>
  <% end %>
</ul>

<%= link_to 'Add a restaurant', new_restaurant_path %>
